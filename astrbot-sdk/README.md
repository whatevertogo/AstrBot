# AstrBot SDK

AstrBot 插件开发 SDK，提供 v4 runtime、worker protocol 和插件工具链。

## 安装

```bash
pip install astrbot-sdk
```

## 开发安装

```bash
# 克隆仓库后
pip install -e .

# 或使用 uv
uv sync
```

## 目录结构

```
astrbot-sdk/
├── src/
│   └── astrbot_sdk/      # SDK 主包
├── pyproject.toml
└── README.md
```
